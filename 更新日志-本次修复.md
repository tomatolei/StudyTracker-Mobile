# StudyTracker-Mobile 本次修复更新日志

更新时间：2026-03-20
整理人：AI 助手

---

## 一、这次修改的目标

本次修改主要围绕下面几个问题展开：

1. **GitHub Actions 构建目录错误**，导致仓库云端打包不稳定甚至直接失败。
2. **项目依赖外部 CDN**，安卓 APK 在离线、弱网、或 CDN 不可用时可能白屏。
3. **日期使用 `toISOString()`**，在东八区场景下可能出现日期偏移。
4. **上传脚本使用 `master` 分支并把 token 写进 remote URL**，存在分支不统一和凭证残留问题。
5. **签名 keystore 被 Git 跟踪**，存在安全风险。
6. **README 文档与项目实际结构不一致**。

---

## 二、本次实际修改文件

本次修改/新增了以下内容：

### 已修改文件
- `.github/workflows/android-build.yml`
- `www/index.html`
- `upload-to-github.bat`
- `README.md`

### 已新增文件
- `www/lib/vue.global.prod.js`
- `www/lib/chart.umd.min.js`
- `www/lib/xlsx.full.min.js`
- `更新日志-本次修复.md`（本文件）

### Git 跟踪状态已调整
- `studytracker.keystore` 已执行 **取消 Git 跟踪**（仅取消版本管理，不删除本地文件）

---

## 三、详细修改内容

---

### 1）修复 GitHub Actions 的构建目录问题

#### 原问题
原来的工作流在 `app/` 子目录中执行：

```yaml
cd app
cordova platform add android@12 || true
cordova build android --release ...
```

但你的仓库实际是 **Cordova 项目就在仓库根目录**，关键文件都在根目录：

- `config.xml`
- `www/`
- `package.json`

所以 GitHub Actions 在 `app/` 目录构建，本质上目录就错了。

#### 本次修改
把工作流改成在 **仓库根目录** 直接构建，不再 `cd app`。

#### 修改点
- 构建触发分支从：
  ```yaml
  branches: [ main ]
  ```
  改为：
  ```yaml
  branches: [ main, master ]
  ```

- keystore 恢复路径从：
  ```yaml
  app/studytracker.keystore
  ```
  改成：
  ```yaml
  studytracker.keystore
  ```

- 新增依赖安装步骤：
  ```yaml
  - name: Install npm dependencies
    run: npm install
  ```

- `cordova platform add` 和 `cordova build` 不再进入 `app/` 目录，直接在根目录执行。

- 构建产物上传路径从：
  ```yaml
  app/platforms/android/app/build/outputs/**/*.apk
  app/platforms/android/app/build/outputs/**/*.aab
  ```
  改成：
  ```yaml
  platforms/android/app/build/outputs/**/*.apk
  platforms/android/app/build/outputs/**/*.aab
  ```

#### 修改后的意义
这一步是**解决 GitHub 打包失败的核心修复**之一。

也就是说，你之前“GitHub 打包不成功，最后只能本机 Android Studio 打包”，很大概率就是这里导致的。

---

### 2）把前端依赖从 CDN 改为本地静态文件

#### 原问题
`www/index.html` 原来直接引用外部 CDN：

```html
<script src="https://cdn.jsdelivr.net/npm/vue@3/dist/vue.global.prod.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
```

这会导致：

- 打包成 APK 后，APP 首次打开仍依赖公网
- 无网络、弱网、CDN 被拦截时可能白屏
- 与“本地离线应用”的定位矛盾

#### 本次修改
新增目录：

```text
www/lib/
```

并下载放入以下本地依赖：

- `www/lib/vue.global.prod.js`
- `www/lib/chart.umd.min.js`
- `www/lib/xlsx.full.min.js`

然后把 `index.html` 中的引用改成本地文件：

```html
<script src="lib/vue.global.prod.js"></script>
<script src="lib/chart.umd.min.js"></script>
<script src="lib/xlsx.full.min.js"></script>
```

#### 修改后的意义
这样打包出来的 APK：

- **不再依赖公网 CDN 才能启动**
- 离线可打开
- 更符合你这个项目“本地管理、离线使用”的实际场景
- 稳定性明显比 CDN 方案高

---

### 3）修复日期偏移问题（`toISOString()` → 本地日期函数）

#### 原问题
代码中原来多处用了：

```js
new Date().toISOString().split('T')[0]
```

这个写法得到的是 **UTC 日期**，不是中国本地日期。

在东八区下，尤其是凌晨附近，会出现：

- 本地已经是今天
- 存档、归档、历史记录仍被算成昨天

这会影响：

- 每日归档
- 异常备注归属日期
- 历史记录分组
- 7 天统计
- 备份/导出文件名

#### 本次修改
在 `www/index.html` 中新增了统一的本地日期工具函数：

```js
const formatLocalDateKey = (source = new Date()) => {
    const date = new Date(source);
    const year = date.getFullYear();
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');
    return `${year}-${month}-${day}`;
};
```

并替换了多个关键位置，包括但不限于：

- 自动归档逻辑中的 `todayKey`
- 自动归档逻辑中的 `yesterdayKey`
- 历史完成项 `itemDate`
- `archiveToday()` 中的 `dateKey`
- 备份文件名 `backupFileName`
- Excel/CSV 文件名 `fileName`
- 保存异常备注时的 `todayKey`
- `syncToHistory()` 中的 `todayKey`
- 最近 7 天统计中的 `dateKey`

#### 修改后的意义
现在日期会按**本地时间**计算，不再受 UTC 影响。

这属于一个看起来小，但其实非常重要的稳定性修复。

---

### 4）修复上传脚本：统一分支为 `main`

#### 原问题
原脚本 `upload-to-github.bat` 里是：

```bat
git branch -M master
git push -f origin master
```

而 GitHub Actions 工作流原来监听的是 `main`。

这就会导致：

- 你本地推的是 `master`
- Actions 监听的是 `main`
- 结果就是 **你明明推了代码，但云端工作流不触发**

#### 本次修改
把上传脚本中的分支统一为 `main`：

```bat
git branch -M main
git push -f origin main
```

同时保留工作流兼容 `master`：

```yaml
branches: [ main, master ]
```

#### 修改后的意义
这样做以后：

- 本地主分支统一为 `main`
- 老仓库如果还有 `master` 推送，也依然能触发工作流
- 分支不一致的问题被消除

---

### 5）修复上传脚本中的 token 使用方式

#### 原问题
原脚本直接把 token 写进 remote URL：

```bat
set "REPO_URL=https://!GITHUB_TOKEN!@github.com/tomatolei/StudyTracker-Mobile.git"
git remote set-url origin "!REPO_URL!"
```

这会导致：

- `.git/config` 里长期保留带 token 的 URL
- token 可能被截图、录屏、备份、误传
- 本地安全性较差

#### 本次修改
脚本改成了两段式：

1. 推送前，临时使用带 token 的 URL：
   ```bat
   set "AUTH_REPO_URL=https://!GITHUB_TOKEN!@github.com/tomatolei/StudyTracker-Mobile.git"
   git remote set-url origin "!AUTH_REPO_URL!"
   ```

2. 推送完成后，恢复为不带 token 的普通 URL：
   ```bat
   set "REPO_URL=https://github.com/tomatolei/StudyTracker-Mobile.git"
   git remote set-url origin "!REPO_URL!"
   ```

#### 修改后的意义
这样做虽然仍然使用 token 推送，但不会让 token 长期留在 Git 远程配置中。

比原先方案干净很多。

> 说明：这已经比原方案安全，但如果以后你想更稳，还是建议改成 `gh auth login` 或 Git Credential Manager 方案。

---

### 6）处理 keystore 被 Git 跟踪的问题

#### 原问题
你仓库中的 `studytracker.keystore` 已经被 Git 跟踪。

即使 `.gitignore` 里有：

```gitignore
*.keystore
```

也只能阻止“以后新增跟踪”，**不能阻止已经被提交过的文件继续被 Git 管理**。

#### 本次修改
我已经执行了：

```bash
git rm --cached studytracker.keystore
```

这一步的效果是：

- **Git 不再跟踪这个 keystore**
- **本地文件仍然保留，不会删除你磁盘上的 keystore**

#### 现状说明
现在它在 Git 里表现为：
- 被移出版本控制
- 本地文件仍然存在
- 以后因为 `.gitignore` 已经写了 `*.keystore`，默认不会再被重新加入

#### 额外安全提醒
这次只是“停止继续泄露”。

如果这个 keystore 之前已经推送到 GitHub，尤其是：
- 仓库曾经公开
- 或你把仓库给别人看过
- 或这个 keystore 已经出现在旧提交历史里

那它实际上已经算**泄露过**。

最稳妥的做法是：

1. **重新生成新的正式发布 keystore**
2. 后续正式版 APK 改用新的 keystore 签名
3. 老 keystore 不再用于正式发布

---

### 7）同步修正文档 README

#### 原问题
README 里有几处与实际项目不一致：

- 写的是 `build-apk.yml`
- 实际文件是 `android-build.yml`
- 工作流名称描述也不够准确
- 分支说明还是老状态
- 没说明上传脚本已经改成 `main`

#### 本次修改
README 已经同步更新：

- 工作流文件名改为：
  ```text
  android-build.yml
  ```
- 工作流名称说明改为：
  ```text
  Build Signed Android APK
  ```
- 分支建议改成优先使用 `main`
- 增加提示：`upload-to-github.bat` 会规范为 `main` 并在推送后恢复远程 URL，避免 token 留在 `.git/config`

#### 修改后的意义
至少以后你自己回来看 README，不会再被旧说明误导。

---

## 四、这次没有直接改动、但你要知道的事

### 1）Excel 导出目前本质上还是 CSV 复制方案
虽然项目里加载了 `xlsx.full.min.js`，但当前 `exportExcel()` 实现仍然是：

- 拼接 CSV 文本
- 复制到剪贴板
- 提示用户自行保存成 `.csv`

也就是说：

- 现在并不是真正的 `.xlsx` 多工作表导出
- 文档中的“Excel 4 个工作表”描述仍然偏理想化

这次我**没有继续重构这块逻辑**，只是保留现有功能，先把更关键的问题修掉。

如果你要，我下一步可以单独把这一块做成真正的：

- `.xlsx` 文件
- 多个工作表
- 直接下载/保存

---

### 2）GitHub Actions 能不能 100% 成功，还取决于 secrets 是否配置完整
工作流已经改对了，但如果 GitHub 仓库里没有配置以下 secrets，云端仍然会失败：

- `KEYSTORE_BASE64`
- `KEYSTORE_PASSWORD`
- `KEY_ALIAS`
- `KEY_PASSWORD`

也就是说：

- **工作流结构问题我已经修了**
- **但仓库 secrets 需要你自己确认是否配全**

---

### 3）你本地 `build.ps1` 目前还是未跟踪状态
当前仓库状态里，`build.ps1` 还是一个未跟踪文件。

本次我没有处理它，因为不确定你是不是故意只做本地使用。

如果你想，我也可以下一步帮你判断这个脚本：
- 要不要提交
- 要不要加入 `.gitignore`
- 有没有敏感信息

---

## 五、现在项目达到的状态

经过这次修改，项目已经从“能跑，但工程上有明显坑”提升到“结构更合理、打包链路更顺”的状态。

### 已解决的核心问题
- GitHub Actions 构建目录错误
- main/master 分支错位
- 外部 CDN 依赖导致 APK 不稳定
- 日期 UTC 偏移
- 上传脚本 token 残留风险
- keystore 继续被 Git 跟踪
- README 与实际结构不一致

### 仍建议后续继续处理的内容
- 把 CSV 导出改成真正的 `.xlsx` 导出
- 统一 README / help-doc / 页面内嵌帮助文档的版本说明
- 如果仓库曾公开，重新生成新的发布 keystore
- 如有需要，进一步本地验证一次 `cordova build android`

---

## 六、建议你接下来怎么做

建议你按下面顺序继续：

### 第一步：先提交本次修改
建议提交以下内容：

- 工作流修复
- `www/lib` 本地依赖
- `index.html` 日期修复
- 上传脚本修复
- README 修复
- keystore 从 Git 移除

### 第二步：检查 GitHub 仓库 secrets
确认以下是否已配置：

- `KEYSTORE_BASE64`
- `KEYSTORE_PASSWORD`
- `KEY_ALIAS`
- `KEY_PASSWORD`

### 第三步：再试一次 GitHub Actions 打包
如果这次还失败，那就不再是原来的“目录错了”问题，而是具体环境或签名参数问题，到时就能有针对性排查。

### 第四步：考虑重置正式发布 keystore
如果这个仓库之前上过公网，我建议正式版签名密钥换新。

---

## 七、补充说明

本次修改中，曾有一次 PowerShell 临时命令尝试失败，但随后已改用更稳定的方式完成文件修改，**最终文件内容已修正，不影响当前结果**。

---

## 八、结论

这次不是简单“改几行字”，而是把你这个项目里最影响实际使用和后续维护的几个坑给补上了：

- 云端打包链路更合理了
- 离线 APK 更像真正的离线 APP 了
- 日期逻辑不再容易出错
- Git/GitHub 使用方式更干净了
- keystore 风险至少先止血了

如果你要，我下一步可以继续帮你做两件事里的一个：

1. **把导出功能重构成真正的 Excel 多工作表 `.xlsx`**
2. **帮你整理一个“提交到 GitHub 的标准提交说明 + 提交顺序”**
